# -----------------------------------------------------------
# Caching
# -----------------------------------------------------------
__pycache__/
*.pyc

# -----------------------------------------------------------
# Testing
# -----------------------------------------------------------
.coverage
coverage.xml
.mypy_cache/
.ruff_cache/
.pytest_cache/

# -----------------------------------------------------------
# Virtual environments
# -----------------------------------------------------------
.venv/
.env
.env.local
/env/
venv/
/ENV/
set-env.ps1

# -----------------------------------------------------------
# Distribution / packaging
# -----------------------------------------------------------
# env/ (and ENV/, which matches it on case-insensitive systems), build/, downloads/, lib/,
# parts/ and var/ are anchored to the repo root: unanchored they would also ignore, for example,
# frontend/src/lib or frontend/src/features/build.
.Python
/build/
develop-eggs/
dist/
node_modules/
/downloads/
eggs/
.eggs/
/lib/
lib64/
/parts/
sdist/
/var/
wheels/
*.egg-info/
*.egg

# -----------------------------------------------------------
# IDE / Editor (additional)
# -----------------------------------------------------------
# Cursor / Bugbot artifacts
.cursor/

# -----------------------------------------------------------
# Claude Code local settings
# -----------------------------------------------------------
.claude/
.claude/settings.local.json
.claude/scheduled_tasks.lock
graphify-out/
.agents/
.impeccable/
PRODUCT.md
DESIGN.md
skills-lock.json

# -----------------------------------------------------------
# Local debug / scratch scripts / data (not for commit)
# -----------------------------------------------------------
debug/
data/

# -----------------------------------------------------------
# Docker
# -----------------------------------------------------------
# Docker compose project directories
docker-data/
